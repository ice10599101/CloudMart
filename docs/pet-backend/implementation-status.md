# 宠物模块后端整改与新增功能 — 实施状态报告（M1–M5 交付）

> 依据：`.ice/宠物模块后端整改与新增功能开发任务书.md`
> 更新：2026-09-26 第二轮交付
> 环境：JDK 26.0.1 / Maven 3.9.15；迁移已在**远程真实 MySQL 9.7** 全链验证

## 0. 诚实声明（仍未完成 / 未验证）

- **T01–T46 验收矩阵**：并发 20 次购买、进程重启续结算、MQ 重复投递、Redis 故障注入等**未执行**（需 Testcontainers 双服务联调环境），不能标记为通过；单测已覆盖等价逻辑路径（见 test-report.md）。
- **性能基线（§9.2）**：合成数据压测（1 万用户/100 万事件）未执行。
- **业务日全量切库（§7.3）**：保持"UTC 存储 + businessZone 归属"的暂缓方案，切换日重叠区额度处理为未完成项。
- B21 配置数值上下限校验、草稿/发布/版本回退接口：部分（新增了管理端操作/举报/补算端点与代理；既有配置 CRUD 校验未逐项加严）。
- B09 扫描批次退避告警、B22 全量指标埋点：部分（RAND 已去、日志字段已含关键字段；Micrometer 指标未接线）。

## 1. 任务完成状态

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| B01 | 完成 | 双侧幂等（wish_pet_operation / pet_operation + outbox + 恢复任务 + 补偿退款 + 管理端查询/重试） |
| B02 | 完成 | 每用户一条进行中（V8）+ 显式版本 CAS + 原子增量 + 影响行数检查 |
| B03 | 完成 | claim by activityId（奖励归 activity.petId）、统一活动列表、领养 FOR UPDATE 配额、切主宠事务化（uk 兜底） |
| B04 | 完成 | 独立小数余量、跨阈值分段、触界归零、统一 PetClock |
| B05 | 完成 | 服务端会话计时 + entitled-grant 同事务 + gain 原子落库 + 升级事件 outbox |
| B06 | 完成 | DB 日额度（喂食/玩耍/休息/对战/拜访/点赞/留言）、定时休息状态机、actions 端点、忙碌互斥 |
| B07 | 完成 | 字符串 ID（VO/MQ/Feign/管理代理）、RFC3339 UTC、业务日 |
| B08 | 完成 | seed 有符号（V12）、稳定野生模板 templateId、待应战分页、双方各一次通知、快照回放（Round 含 actorPetId/targetPetId） |
| B09 | 完成 | 开始冻结规则快照（V13）、按 ID 领取、领取截止时间、扫描每分钟推进（退避告警部分完成） |
| B10 | 完成 | 任职 stint（V14）、入职仅一阶、晋升统一校验 + 幂等扣款 + 恢复补投递 |
| B11 | 完成 | 冷却独立、种子冻结结果、独立事务结算 Bean、远程 requestId 幂等（wish V39） |
| B12 | 完成 | 原始自定义外观/卸肤恢复/手动改色同步背包、同槽函数唯一索引（V16）、替换预览、技能槽配置开关、三类物品拥有判断 |
| B13 | 完成 | 每家具单实例、舒适度封顶、点赞持久化+显式取消、回家奖励 DB 化、历史归档迁移 |
| B14 | 完成 | 拜访统一结算（共享 10+好友 5 子额度）、屏蔽名单、举报+管理审计、关系无向去重（V15）、好友接受重验名额、留言 DB 限额 |
| B15 | 完成 | 冻结任务集合（当日新增配置次日生效）、CANCELLED 显式取消不卡宝箱、宝箱门禁排除取消项 |
| B16 | 完成 | LIFETIME/WINDOW 显式模式、开始前不可领、结束后 24h 领取宽限、BOTTLE 只计 CAUGHT |
| B17 | 完成 | 陪伴时长触发（不依赖升级）、聊天第 N 次当次触发、管理端补算端点（幂等） |
| B18 | 完成 | AI 调用移出 DB 事务（TransactionTemplate 两短事务）、消息/AI 双额度拆分、Idempotency-Key 请求幂等（V18）、危机词/降级保留 |
| B19 | 完成 | eventId 去重（V5+幂等落库）、PET 类型全部已读、可靠投递（outbox）、消息体字符串 ID |
| B20 | 完成 | 先过滤后排名、同分稳定序（cnt/level desc, id asc）、私密 rank=null、剔除野生模板 |
| B21 | 部分 | 操作查询/重试、举报处理、成就补算端点 + mall-admin 全量代理；配置数值校验未逐项加严 |
| B22 | 部分 | 邻居随机偏移抽样替代 ORDER BY RAND()、结构化日志字段；指标埋点未接线 |
| N01 | 完成 | 引导进度（领域事件驱动：喂食/玩耍/摆放接线）、跳过幂等、新手家具赠送走操作记录幂等 |
| N02 | 完成 | 日记（outbox 单 choke point 自动生成、eventId 去重、不可变快照）、相册（归属校验/100 张限额/删除）、时间线游标分页 |
| N03 | 完成 | 记忆列表/编辑（USER 优先）/删除标记防复活/批量清空/提取与使用双开关；归属校验 |
| N04 | 完成 | 接球局（服务端随机序列/窗口校验/宽限/训练局）、结算 CAS 幂等、额度预占（每日 5 局+玩耍额度）、历史 |
| N05 | 完成 | 托管（周额度 uk/最长 24h/互斥）、惰性照顾去重（饱食 2 次/清洁 1 次）、提前结束不退次数、不产出养成收益 |
| N06 | 完成 | 合作邀请/接受（周名额双向 uk）、贡献事件去重（每人每日 1 次）、完成判定、退出不恢复名额、隐私最小化 |
| N07 | 完成 | 图鉴配置/解锁幂等（用户累计）、未解锁线索、隐藏条目保护、统计进度 |
| 数据迁移 | 完成 | V7–V18（mall-pet）、V38–V39（mall-wish）、V5（mall-notification）；**远程 MySQL 9.7 全链验证通过**（pet 50 表 / wish 61 表 / notification 链） |
| M5 文档 | 完成 | implementation-status / api-contract / api-examples.http / data-migration / test-report / operations / client-integration 七件套 |

## 2. 测试与验证证据

- 单元测试：mall-pet **102/102**、mall-wish **783/783**、mall-notification **47/47** —— PASS
- 打包：mall-pet / mall-admin / mall-wish / mall-notification `-am package` —— PASS
- 迁移：远程 MySQL 9.7 三链全通（一次性验证库已清理）—— PASS
- T01–T46 并发/故障注入/端到端 —— **NOT RUN**（需联调环境，见 test-report.md）

## 3. 用户侧待办

1. 重启 mall-pet：`FlywayRepairConfig` 会自动清除 V15 失败历史行并重新迁移（V15 SQL 已修复并通过真实 MySQL 验证）。
2. 联调环境跑 T01–T46 关键并发场景后更新 test-report.md。
