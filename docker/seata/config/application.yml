server:
  port: 7091

spring:
  application:
    name: seata-server
  main:
    web-application-type: none

logging:
  config: classpath:logback-spring.xml
  file:
    path: ${log.home:${user.home}/logs/seata}

console:
  user:
    username: seata
    password: seata

seata:
  config:
    type: file
  registry:
    type: nacos
    nacos:
      application: seata-server
      server-addr: nacos:8848
      namespace:
      group: SEATA_GROUP
      username: nacos
      password: nacos
      ip: ${SEATA_IP:127.0.0.1}
  server:
    service-port: 8091
  store:
    mode: file
  security:
    secretKey: ${SEATA_SECRET_KEY:}
    tokenValidityInMilliseconds: 1800000
    csrf-ignore-urls: /api/v1/auth/login
