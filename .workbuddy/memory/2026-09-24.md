# 2026-09-24

## 新猫融入家园设计稿（画布 728925154478829）
- 按设计尺寸 1:1 重截底图：Web 1440×900（room 机位）→ `shots/v8/web_home.png`；
  移动 390×844（**必须 --shots portrait**，URL 带 shot=room 会用横屏机位导致猫被裁切偏移）
  → `shots/v8/mobile_home.png`。
- 画板节点：Web 底图=2:699，移动底图=2:886（IMAGE 填充 + scaleMode FILL）。
- upload_images 会把 filters 重置为 0 —— 替换底图后必须重设调色
  （exposure −8 / contrast +6 / saturation −5 / temperature 6 / tint 2 / highlights −16 / shadows 4），
  且重设时要带上**当次 upload 返回的新 imageHash**（web: 084308d2…，mobile: 9bf38e3e…）。
- 验收截图存 `.workbuddy/tmp/ardot_web.png / ardot_mobile2.png`。

## 融入后评审待改清单（已列给用户）
1. 地毯中心圆与奶油灰猫毛色明度对比偏低 → 建议地毯加深一档（PetRoomBuilder）或底图加 contrast。
2. 需求卡与猫之间无视觉连接 → 可选加 12px 指引短线（卡片左缘指向猫）。
3. Web 左下蓝球动画位偶发被"陪它玩一会儿"按钮遮住 → 可把玩具球摆位左移（roomAmbience/PetRoomBuilder）。
4. 移动端底部面板盖住猫下半身约 1/4 → 符合"面板可上滑"意图，如要露爪把面板顶部下移 24px。

## ⚠️ 回归修复：猫变灰黑（评审打回"越做越差"）
根因是 ⑫ 的修复引入的回归链：暗部改 `shadeColor×base` 后，pet-toon 的主光仍是
"来自窗户（背后）"→ 猫脸朝镜头 lit≈0 → **整只猫 100% 落在 shadeColor 里**，
琥珀眼/玫瑰围巾被同一层暖灰棕乘掉 → 全身灰黑、质感生硬。
（画布底图又叠了旧调色 preset（exposure −8/saturation −5，为过曝粉白房间调的）雪上加霜。）
修复（PetGameRoot.configurePetMaterials，全部 setProperty 即可，无需改 effect）：
- `lightDir` 改宠物专属"左前上" (-0.42,0.60,0.68)（与房间同源 -x/+y 方位但照亮可见面）；
  `fillDir` (0.30,-0.20,0.92)；角色与场景各带一套主光是常态。
- `shadeColor` 抬亮到 (206,190,172)；`shadeCtrl.y` 0.24→0.34 加宽羽化去硬色阶；
  `furCtrl` (0.16,2.0,0.04,6.0) 高光更宽更弱；`lightCtrl` (0.90,0.22,0.10,0.10)。
- 画布底图调色改轻手：exposure 0 / contrast 4 / saturation 2 / temperature 2 / highlights −8 / shadows 4。
**教训：房间与角色共用 effect 时，lightDir/shadeColor 这类"光照语言"参数必须按对象分 Material 设置，
不能只共享 effect。** 新底图：shots/v9/check_web.png(1440×900) / check_mobile.png(390×844)，
画布 hash web=cce568cb… mobile=36feb1e1…。

## 🔄 重大方向修正：回到原始猫"奶盖"，只改尾巴（用户拍板）
用户评审：重新生成的 v2 猫"惨白、生硬、配饰和眼睛糊成灰黑"，而
`pet-game/模型/pet_cat_viewer.html` 引用的 **pet-cat-runtime.glb（2.21MB / 11,994 面）**
才是他认可的版本 —— "我只是要改尾巴，整只都不一样了"。
**教训：用户认可的造型是资产基准，改局部（尾巴）绝不能用"重新生成整只"来实现。**

### 最终管线（rig_cat_v1_tail.py，全部实测坐标）
- 骨骼：Root/Hips/Spine/Chest/Neck/Head + 双耳×2（rig_cat.py 实测表）+ Tail1..11（新尾路径）
- 旧绕身尾（-X 侧低处，x -0.373..-0.220，z≤0.276，与体表交织）：**不删（删必撕）**，
  沿径向收缩到轴心 15% 塌进躯干内部（走廊 d<0.075 全收 + d 0.075~0.14 渐变收）；
  拟合线必须**密桶（61）**—— 7 点粗线的弦切误差会漏掉尾弧 → 悬空残片（已实测）。
- 新尾：屁股后锥形管（路径向 +X 弯让游戏机位可见，Chaikin 平滑 6→11 控制点，
  segs=18），UV 从旧尾顶点原位继承（虎斑环纹延续）；bind_tail 按路径参数解析绑权重。
- **尾骨半径必须 0.06 系**（0.09 会把臀部顶点抓给尾骨）；**链式旋转沿链累加** ——
  每节 22°×11 = 尾尖 350° 拧穿身体（已实测）；动画按"尾尖总弯角÷节数"分配
  （key_tail()）：Idle 尾尖 ±16°，Happy 峰值 60°。
- 眨眼：眼球拟合 r≈0.074/0.075（d<0.085 邻域），皮碗 1.05×R、半角 50°、
  **rest 预旋 -100° 藏进头骨**、闭眼 +160°；双眼必须同时定位成功才启用；
  eyelid_meta.json 契约 → animate_cat.py。
- 产物：`design/model/pet-cat-v1-tail.glb`（2.47MB，Idle/Happy/Blink），
  已替换 `assets/resources/models/cat/pet-cat-rigged.glb`；
  PetGameRoot 恢复 v1 参数（PET_HEIGHT=0.9503×1.75，HEAD_OFFSET=(0.05,1.05,0.46)）。
- 材质：宠物专属主光（左前上）+ 浅暖灰暗部对新猫同样适用，银灰虎斑层次正常。
- 画布底图已换（hash web=59b46c06… mobile=0bb6c404…，轻手调色）。
- 工程教训：bmesh 包装对象 id() 不稳定，并查集必须用 e.index 当键（KeyError 已实测）。

## 🎯 全面重做：按官方 QQ 宠物猫（用户拍板，最终路线）
背景：v1+改尾方案用户仍不满意（"原尾位置变形、新尾短节、死气沉沉"）。
用户放话：**全面查询腾讯 QQ 宠物最新版资料，按它们的猫重做，不要再改旧的**。

### 情报（2026-09-24 检索）
- QQ 宠物 2026-07-27 回归（手Q），3D 毛茸茸建模、触摸毛发摆动；物种=企鹅/狗狗/蒜头鹅，
  **官方没有猫**；2026-09-05 有玩家自制"猫咪系列"概念帖（标注 AI 生成非官方）。
- **用户提供了 9 张官方游戏截图**（pet-game/模型/Screenshot_20260924_*.jpg）= 官方猫实拍：
  奶油杏色长毛猫（喜马拉雅型）、无任何配饰、尖耳+耳毛+粉内耳、
  **超大的深棕圆眼带琥珀环+大高光**、粉鼻、头约占坐高一半、绒毛轮廓带毛发丝、
  蓬松尾巴卷在身侧。这 9 张图就是建模基准。
- 资料位置：design/concept/Isolate_this_exact_cream_fluff_*.png（图生图净版，已裁水印 →
  qqcat_concept_clean.png 1024×960）；参考图备份在 .workbuddy/tmp/qqpet/。

### 管线进行中
- gen3d_v2.py 提交图生 3D（300k 面、PBR）→ qqcat/gen3d_result.json。
  **⚠️ gen3d 必须用系统 Python**（D:/Program Files/Python314/python.exe，有 requests）；
  托管 Python 无 requests 且离线装不了（_ensure_requests 会失败）。
- 下游：produce（仿 produce_v2.sh）→ 量测/绑骨（v2 管线的眼球拾取+眨眼经验直接复用；
  尾巴这次是**原生建模尾巴**，预计不用删尾重建）→ 动画 → 部署 → 画布。

### ✅ QQ 风格猫全管线完成（当日收口）
- 图生 3D：3m39s 完成（40 额度），raw 49.7MB → produce（simplify 0.045 / 1024 / jpeg /
  tangents / fix）→ qqcat-prod.glb 2.16MB（11,525 面 / 384 碎片岛）。
- 量测：高 0.8028、脸朝 -Y、眼区 y≈-0.29、原生地面卷尾（+X 侧 x 0.18..0.36 / y 0.19..0.48）。
- 眼球：probe_eye_qq.py（正/(0.10,-1.90,0.60) look(0,-0.20,0.52) lens50）→
  eye_pick.json（+X 拟合 r=0.041；-X 拟合失稳 r=0.029 → 大眼半径+单射线定心）。
- 绑骨 rig_qqcat.py：13 变形骨（躯干5+耳4+尾4）；**耳骨从几何自动量测**（z>0.62 按 ±x 分簇）；
  原生尾直接绑（骨半径 0.05-0.07 + below 门控）；眼皮**月牙形**（half-angle 38°——
  纯色贴图上 50° 碗读作凸球，38° 是"眼睑线"）+ rest -100° + 闭眼 145°。
- 毛色 uv 探针改"band 中位色最近"（挑 cream 会采到高光区 → 闭眼读凸球，已实测）。
- animate：key_tail() 按"尾尖总弯角÷节数"分配（链式累加教训通用化）。
- 部署：qqcat-rigged.glb 2.35MB → assets（Idle/Happy/Blink）；
  PetGameRoot：SCALE=1.9（0.8028×1.9≈1.53）、HEAD_OFFSET=(-0.05,0.99,0.49)。
- 画布：D/E 底图 hash web=441746b5… mobile=68d2c328…。
- 游戏内效果与用户提供的官方截图高度一致（奶油绒毛/大琥珀眼/卷尾）。

### "惨白没生气"根因链（QQ 猫上线后用户仍不满意 → 已修）
1. **贴图本身比官方参考白**（生成概念漂白）→ tint_qqcat.py 在贴图层校色：
   饱和 +45% + 暖移 (0.96,0.85,0.74)（对齐官方 #E8D3B6）。
   ⚠️ 只校 baseColor —— 法线/金属粗糙度贴图是方向与参数数据，当颜色调会毁着色（已踩）。
2. **可见面动态范围被压缩**：半兰伯特下正面法线 ndl∈[0.5,1.0]，shadeCtrl 阈值低时
   暗部全落在看不见的背面 → 调什么都平。shadeCtrl=(0.72,0.12) 对准可见区间 +
   lightDir 左侧强侧光 (-0.85,0.30,0.32) → 左亮右暗大转折（量化验证：暗侧/亮侧=0.80）。
3. **剩余差距**：官方引擎的柔和渐变着色 + 褶皱 AO + 眼睛湿润感，pet-toon 的
   双段 toon 近似不了 —— 需要升级 pet-toon（多级渐变 + 假 AO 项），待用户定。
- 最终材质：lightDir(-0.85,0.30,0.32) fillDir(0.42,-0.18,0.86) shadeColor(166,148,130)
  shadeCtrl(0.72,0.12,0.16,0.12) lightCtrl(0.88,0.28,0.14,0.14) furCtrl(0.16,2.0,0.06,10)。
- 验收截图 shots/v13/tinted2_web.png + tinted_mobile.png；画布 hash web=b35cde91… mobile=155cf60d…。

## 😺 "生动感"再提升（贴图重绘，不重建 GLB）—— 用户"没原型图生动"
用户：游戏内猫不如最初 2D 原型概念图（Isolate_this_exact_cream_fluff_*.png）生动。

### 关键发现（实测）
- 部署 `assets/resources/models/cat/pet-cat-rigged.glb`（3.49MB）被引擎管线改过，
  **Blender 5.2 无法导入**（"Bad glTF: json error: utf-8"）→ 不能走"重新导出 GLB"。
- 裸解析（stdlib）从 GLB 二进制块抽 3 张内嵌 PNG：
  img_0=法线图(0.5,0.5,1.0) / **img_1=身体 albedo**(暖奶油 0.76,0.58,0.42，双眼 uv≈(0.101,0.250)&(0.588,0.250)，
  暗瞳孔 0.157,0.106,0.078；额头(0.34,0.45)奶油；v=0.50~0.55 转粉=内耳→脸正立，鼻嘴在眼下方) / img_2=metal/rough(饱和琥珀)。
- GLB 结构：3 网格(EyelidLMesh/EyelidRMesh/node_0)**共用 Material.001**，都采样 img_1 → 改 img_1 一处即全员跟随。

### 做法（已构建通过）
1. `alive_redraw_png.py`：在 img_1 上重绘**大琥珀眼**(虹膜230,150,45/瞳孔35,24,18/湿润高光/细上眼线)
   + 粉鼻(255,150,165)+微笑嘴线+眼下腮红；降采样 768²（UV 分辨率无关）使文件 < 槽位。
2. `splice_glb.py`：重绘 PNG **原地替换**进 bufferView 7（绝对偏移 742520，长度 1155761，零填充补齐），
   JSON/偏移/长度不动；原 GLB 备份为 `pet-cat-rigged.glb.bak`。
3. `cocos-cli build` 成功(16s，图片压缩过)；`shot.mjs` 截 `shots/v15/room.png`/`front.png`。
   控制台确认 3 网格都用重绘 albedo(albedoTex=9d153a98…@221a5)+Idle/Happy/Blink；wasm/404 是 headless spine 桩老问题，无关。

### 验证（数值，图片被过滤无法肉眼）
重绘后 uv 抽样：瞳孔暗0.145,0.098,0.071；虹膜琥珀0.904,0.635,0.286；高光白0.994；鼻粉0.963,0.646,0.665；
额头奶油0.835,0.587,0.500 → 重绘正确落位。截图非空白（lum_std≈0.17~0.19）。

### 待办/风险
- **用户需肉眼判生动感是否达标**（AI 看不到图）。不够则备选：billboard sprite 眼（eye_sprite.png 已生成但缺 .meta 未接）。
- 生产若从 design/model 覆盖 assets 的 GLB，需把同样 splice 也应用到 design 源，或改走资产侧重绘纹理。

---

## 2026-09-25 凌晨续：白猫修复落地 + 构建链三层根因排查（重要）

### 一、白猫问题的最终方案（已落地，弃用 GLB 拼接）
- 弃用 `bake_alive.py` 的 prVw/GLB 拼接路线（对 Cocos 导入器兼容性始终未真机验证）。
- 新方案：**独立贴图资产 + 运行时覆盖**：
  - `bake_alive.py` 的干净产物 `_probe/redraw_debug.png`（1024²，无填充）
  - `tools/asset-pipeline/make_alive_texture.py`：解码自检（非白板 std>0.05 / 双眼深色 / 粉鼻）→
    安装为 `assets/resources/textures/pet-cat-alive.png`（meta 由构建 asset-db 自动生成，eye_sprite 先例）
  - `PetGameRoot`：材质前置加载 `textures/pet-cat-alive/texture`，`configurePetMaterials`
    优先用 `aliveTexture`，失败回退 GLB 内嵌 albedo（永不白板）
  - 数据自检通过：mean RGB=[0.764,0.58,0.421] 奶油色、眼 (0.101,0.25)/(0.588,0.25) 处 lum=0.109、粉鼻 [0.957,0.576,0.604]
  - preview 探针证实：三个网格 albedoTex=02825ce1(pet-cat-alive) ✓
- 已部署 GLB 保持 CLEAN 原版；prVw 版仍在 `_probe/backups/pet-cat-rigged.prVw.glb` 备而不用。

### 二、"构建产物黑屏"= swiftshader 环境限制，不是回归（定案）
- 9/19 的**原始未触碰构建**（pet-game/build/web-mobile）在本机 headless Edge/chromium+swiftshader
  下同样全黑、同样 program not found → 构建产物路径在软件 GL 下**从未渲染过**。
- 工作时代截图（9/24 等）来自用户真机浏览器或 preview，不是 swiftshader。
- 推论：构建产物对用户浏览器没有回归；headless 验收必须走 **cocos preview**（动态服务引擎源码）。
- 定案工具：`tools/asset-pipeline/_probe/shot_edge.mjs`（playwright-core + 本机
  chromium-1234，executablePath；NODE_PATH 指向 workspace/node_modules；抓 console/pageerror/http+截图）。
  playwright-core@1.63.0 装在 `C:/Users/Administrator/.workbuddy/binaries/node/workspace`。

### 三、spine 初始化缺陷的根修（替代旧 index.html v1 桩）
- 缺陷：vendored 引擎基础 init 无条件加载 spine asm/mem 并实例化，asm 实例化抛
  embind/BindingError；该链被 `cc.game.init` await → 中断 → programLib 全空 → 黑屏。
- 旧 v1 桩（index.html 补 SystemJS.import 返回 {default:空函数}）失败根因：
  引擎 `nHt` 要求 `t.SpineWasmUtil`（undefined→"setting 'wasm'"崩）；且 Promise.resolve
  对假库探测 .then（v2 桩曾因 Proxy 对 then 返回函数 → 被当未决 thenable → 无声卡死）。
- **根修**：`build-templates/web-mobile/cocos-js/spine.asm-t8kGod18.js` 用构建模板覆盖机制
  把有缺陷的 asm 分块替换为"惰性假工厂"（Proxy 哑库：ownKeys 提供全部 spine 类名供 nHt 的
  for-in 拷贝进引擎命名空间；get 对 then/catch/finally 返回 undefined；SpineWasmUtil 空实现）。
  重构建自动覆盖，无需手工。本工程无 spine 资产，哑库永不被业务触碰。
- 验证：preview 下 BindingError=0、wasm 崩溃=0、链路完整走通。

### 四、preview（动态服务）的两个形态差异（已适配）
- `AmbientInfo.skyColorHDR` 在 preview 源码引擎形态是**只读 getter**，直接赋值抛 TypeError
  并中断 buildScene → 猫不出现。已在 `buildLighting` 加可写探测 + try 语义守卫（hdrWritable=false 时 LDR 单写）。
- **TS 改动必须重启 preview 服务**才生效（chunk 编译缓存不随源码失效）。
- preview 下蒙皮网格（pet-cat-rigged）不渲染：节点/材质/layer 全部正常但不可见，
  raw glTF 材质（?rawmat=1）同样不可见 → preview 怪癖（用户浏览器此前渲染过同一模型）。
  诊断参数：`?probe=1`（dump 节点树）、`?probeMat=1`、`?rawmat=1`。

### 五、构建系统事实（排查中确立，防止再绕弯）
- 模块配置有**两份**：`settings/cocos.config.json`（CLI 构建 read）与
  `settings/v2/packages/engine.json`（编辑器用）——改一处不够。
- 引擎编译缓存：`cocos-cli/packages/engine/bin/temp/<hash>`，键含 includeModules/debug、
  **不含 flags**；改 LOAD_*_MANUALLY 需手动删缓存目录才重编。缓存键里 CLI 会自动附加
  `debug-renderer` + `custom-pipeline-builtin-scripts`（后者是内置管线 program 的来源，**不可裁**）。
- `effect.bin`（UBO 布局，5755B）一直存在于产物且无害（与 9/19 版逐字节相同，已证伪非黑屏原因）；
  includeModules 含 custom-pipeline 时构建必写 `effectSettingsPath`。
- asset-db 库：library/temp 可整体 mv 走重建（已做过一次，无变化）；
  safe-delete shim 对 `rm -rf` 无视 env 内联，用 **mv 到别处**代替删除。
- 旧 library/temp（含工作时代 builder 日志）在 `tools/asset-pipeline/_probe/_stale-cache/`，可删。

### 六、遗留
- preview 蒙皮网格不渲染未解（不影响用户浏览器路径，低优先）。
- 最终验收需用户在浏览器打开构建产物确认：奶油猫 + 琥珀眼 + 粉鼻 + 腮红 + 微笑
  （构建已含全部修复，见 `/pet-game/index.html?demo=1`，静态服务器 5199）。
