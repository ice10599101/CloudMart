package com.cloudmart.pet.repository;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.cloudmart.pet.entity.PetCareerStint;
import org.apache.ibatis.annotations.Mapper;

/** PetCareerStintMapper：MyBatis-Plus BaseMapper（复杂查询用 LambdaWrapper，无 XML）。 */
@Mapper
public interface PetCareerStintMapper extends BaseMapper<PetCareerStint> {
}
